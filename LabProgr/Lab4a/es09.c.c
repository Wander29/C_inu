#include <stdio.h>

#define MAX 5

int main()
{
	int a[MAX], b[MAX];

	for (int i = 0; i < MAX; i++)
	{
		while (scanf("%d", &x) < 1 || x <= 0)
		{
			while (getchar() != '\n');
		}
	}
	
	
	for (int i = 1; i <= 10; i ++)
	{
		printf("%d\n", x * i);
	}

	return 0;
}